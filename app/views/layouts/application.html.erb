<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Dayable</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Keep track of your success">
    <meta name="author" content="swlkr">
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le fav and touch icons
    <link rel="shortcut icon" href="../assets/ico/favicon.ico">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="../assets/ico/apple-touch-icon-114-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="../assets/ico/apple-touch-icon-72-precomposed.png">
    <link rel="apple-touch-icon-precomposed" href="../assets/ico/apple-touch-icon-57-precomposed.png">-->
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Dayable</a>
          <div class="nav-collapse">
            <ul class="nav pull-right">
              <li><a href="/about">About Me</a></li>
              <% if user_signed_in? %>
                <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
              <% else %>
                <li class="<%= 'active' if request.url.include? new_user_registration_path %>"><%= link_to "Sign Up", new_user_registration_path %></li>
                <li class="<%= 'active' if request.url.include? new_user_session_path %>"><%= link_to "Sign In", new_user_session_path %></li>
            <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">

      <%= yield %>

      <% if !notice.nil? %>
      <p class="alert alert-info"><%= notice %></p>
      <% end %>
      <% if !alert.nil? %>
      <p class="alert alert-error"><%= alert %></p>
      <% end %>

      <hr />
      <footer>
        <p>&copy; swlkr</p>
      </footer>

    </div> <!-- /container -->
  </body>
</html>
